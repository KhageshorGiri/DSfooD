@model IEnumerable<ProjectFirst.Models.Municipality>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MunicipalityName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.District)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MunicipalityName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.District.DistrictID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.MunicipalityID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.MunicipalityID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.MunicipalityID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
