@model ProjectFirst.Models.Coupen

@{
    ViewData["Title"] = "Create";
}
@using (Html.BeginForm("Create", "Coupens"))
{
    <h2>Create</h2>

    <h4>Coupen</h4>
    <hr />
    <div class="row">
        <div class="col-md-4">
            <form asp-action="Create">
                <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                <div class="form-group">
                    <label class="control-label">CoupenName</label>
                    <input name="CoupenName" class="form-control" />
                    <span asp-validation-for="CoupenName" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label class="control-label">Amount</label>
                    <input name="Amount" class="form-control" />
                    <span asp-validation-for="Amount" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label class="control-label">CoupenCode</label>
                    <input name="CoupenCode" class="form-control" />
                    <span asp-validation-for="CoupenCode" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <input type="submit" value="Create" class="btn btn-default" />
                </div>
            </form>
        </div>
    </div>
}
    <div>
        <a asp-action="Index">Back to List</a>
    </div>

    @section Scripts {
        @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
    }
